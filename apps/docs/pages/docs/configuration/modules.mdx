# Modules Configuration

## Configure

1. Copy `/packages/configs/src/modules.default.ts` to `/packages/configs/src/modules.ts`.

2. Modify `/packages/configs/src/modules.ts` to your needs.

---

## Properties

When you want to use a module, you need to add a property to `/packages/configs/src/modules.ts`.

| Option     | Description                      |
| ---------- | -------------------------------- |
| `name`     | The name of the module.          |
| `position` | The position of the module.      |
| `config`   | The configuration of the module. |

#### Positions

- `top-left`
- `top`
- `top-right`
- `center-left`
- `center`
- `center-right`
- `bottom-left`
- `bottom`
- `bottom-right`
