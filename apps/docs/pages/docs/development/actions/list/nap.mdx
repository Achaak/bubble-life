# Nap actions

## addNapActionInWaitingList()

Add an nap action in the await list.

### Use in app module

```typescript
import { addNapActionInWaitingList } from '@bubble/core';

addNapActionInWaitingList({
  start: new Date().getTime(),
  duration: 10000,
  importance: 2,
  memory: {
    recoverValue: 0.2,
  },
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('addNapActionInWaitingList', {
  start: new Date().getTime(),
  duration: 10000,
  importance: 2,
  memory: {
    recoverValue: 0.2,
  },
});
```

#### Parameters

| Parameter  | Description                  | Type        | Default     |
| ---------- | ---------------------------- | ----------- | ----------- |
| start      | The start time of the action | `number`    | `required`  |
| duration   | The duration of the action   | `number`    | `required`  |
| importance | The importance of the action | `1, 2 or 3` | `required`  |
| memory     | The memory of the action     | `object`    | `undefined` |

| Parameter    | Description          | Type     | Default     |
| ------------ | -------------------- | -------- | ----------- |
| recoverValue | The value to recover | `number` | `undefined` |

---

## addNapActionInWaitingListDefault()

Add an nap action in the await list with default values.

### Use in app module

```typescript
import { addNapActionInWaitingListDefault } from '@bubble/core';

addNapActionInWaitingListDefault();
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('addNapActionInWaitingListDefault');
```
