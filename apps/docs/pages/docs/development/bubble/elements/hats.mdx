import Callout from 'nextra-theme-docs/callout'

# Hats Element

## addHatInList()

Add a hat to the list of hats.

### Use in app module

```typescript
import { addHatInList } from '@bubble/store'

addHatInList({
  id: 'hatId',
  importance: 1,
  name: 'HatName',
})
```

### Use in controller module

```typescript
import { socket } from '@bubble/common'

const socket = socket()
socket.emit('addHatInList', {
  id: 'hatId',
  importance: 1,
  name: 'HatName',
})
```

#### Parameters

| Parameter  | Description                            | Type        | Default    |
| ---------- | -------------------------------------- | ----------- | ---------- |
| id         | Id of the hat to add.                  | `string`    | `required` |
| importance | Importance of the action to check for. | `1, 2 or 3` | `required` |
| name       | Name of the action to check for.       | `Hats`      | `required` |

---

## removeHatInList()

Remove a hat from the list of hats.

### Use in app module

```typescript
import { removeHatInList } from '@bubble/store'

removeHatInList({
  id: 'hatId',
})
```

### Use in controller module

```typescript
import { socket } from '@bubble/common'

const socket = socket()
socket.emit('removeHatInList', {
  id: 'hatId',
})
```

#### Parameters

| Parameter | Description              | Type     | Default    |
| --------- | ------------------------ | -------- | ---------- |
| id        | Id of the hat to remove. | `string` | `required` |

---

## resetHat()

Reset the hat state.

### Use in app module

```typescript
import { resetHat } from '@bubble/store'

resetHat()
```

### Use in controller module

```typescript
import { socket } from '@bubble/common'

const socket = socket()
socket.emit('resetHat')
```

---

## setActionHat()

<Callout emoji="⚠️">This function must follow the nomenclature of the available elements.</Callout>

Set the action hat.

### Use in app module

```typescript
import { setActionHat } from '@bubble/store'

setActionHat({
  actionId: 'actionId',
  name: 'hatName',
})
```

### Use in controller module

```typescript
import { socket } from '@bubble/common'

const socket = socket()
socket.emit('setActionHat', {
  actionId: 'actionId',
  name: 'hatName',
})
```

#### Parameters

| Parameter | Description                | Type     | Default    |
| --------- | -------------------------- | -------- | ---------- |
| actionId  | Id of the action to set.   | `string` | `required` |
| name      | Name of the action to set. | `Hats`   | `required` |

---

## resetActionHat()

Reset the action hat.

### Use in app module

```typescript
import { resetActionHat } from '@bubble/store'

resetActionHat()
```

### Use in controller module

```typescript
import { socket } from '@bubble/common'

const socket = socket()
socket.emit('resetActionHat')
```
