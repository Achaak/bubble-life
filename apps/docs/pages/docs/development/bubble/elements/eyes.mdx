import Callout from 'nextra-theme-docs/callout';

# Eyes Element

## addEyesInList()

Add a eyes to the list of eyes.

### Use in app module

```typescript
import { addEyesInList } from '@bubble/store';

addEyesInList({
  id: 'eyesId',
  importance: 1,
  name: 'EyesName',
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('addEyesInList', {
  id: 'eyesId',
  importance: 1,
  name: 'EyesName',
});
```

#### Parameters

| Parameter  | Description                            | Type        | Default    |
| ---------- | -------------------------------------- | ----------- | ---------- |
| id         | Id of the eyes to add.                 | `string`    | `required` |
| importance | Importance of the action to check for. | `1, 2 or 3` | `required` |
| name       | Name of the action to check for.       | `Eyes`      | `required` |

---

## removeEyesInList()

Remove a eyes from the list of eyes.

### Use in app module

```typescript
import { removeEyesInList } from '@bubble/store';

removeEyesInList({
  id: 'eyesId',
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('removeEyesInList', {
  id: 'eyesId',
});
```

#### Parameters

| Parameter | Description               | Type     | Default    |
| --------- | ------------------------- | -------- | ---------- |
| id        | Id of the eyes to remove. | `string` | `required` |

---

## resetEyes()

Reset the eyes state.

### Use in app module

```typescript
import { resetEyes } from '@bubble/store';

resetEyes();
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('resetEyes');
```

---

## setActionEyes()

<Callout emoji="⚠️">
  This function must follow the nomenclature of the available elements.
</Callout>

Set the action eyes.

### Use in app module

```typescript
import { setActionEyes } from '@bubble/store';

setActionEyes({
  actionId: 'actionId',
  name: 'eyesName',
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('setActionEyes', {
  actionId: 'actionId',
  name: 'eyesName',
});
```

#### Parameters

| Parameter | Description                | Type     | Default    |
| --------- | -------------------------- | -------- | ---------- |
| actionId  | Id of the action to set.   | `string` | `required` |
| name      | Name of the action to set. | `Eyes`   | `required` |

---

## resetActionEyes()

Reset the action eyes.

### Use in app module

```typescript
import { resetActionEyes } from '@bubble/store';

resetActionEyes();
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('resetActionEyes');
```
