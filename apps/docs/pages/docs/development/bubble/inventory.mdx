# Inventory

## addInventoryItem()

Add an item to the inventory.

### Use in app module

```typescript
import { addInventoryItem } from '@bubble/store';

addInventoryItem({
  type: 'InventoryItem',
  number: 1,
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('addInventoryItem', {
  type: 'InventoryItem',
  number: 1,
});
```

#### Parameters

| Parameter | Description                 | Type            | Default    |
| --------- | --------------------------- | --------------- | ---------- |
| type      | The type of the item to add | `InventoryItem` | `required` |
| number    | The number of items to add  | `number`        | `required` |

---

## removeInventoryItem()

Remove an item from the inventory.

### Use in app module

```typescript
import { removeInventoryItem } from '@bubble/store';

removeInventoryItem({
  type: 'InventoryItem',
  number: 1,
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('removeInventoryItem', {
  type: 'InventoryItem',
  number: 1,
});
```

#### Parameters

| Parameter | Description                    | Type            | Default    |
| --------- | ------------------------------ | --------------- | ---------- |
| type      | The type of the item to remove | `InventoryItem` | `required` |
| number    | The number of items to remove  | `number`        | `required` |

---

## hasInventoryItem()

Check if has an item in the inventory.

### Use in app module

```typescript
import { hasInventoryItem } from '@bubble/store';

hasInventoryItem({
  type: 'InventoryItem',
  number: 1,
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('hasInventoryItem', {
  type: 'InventoryItem',
  number: 1,
});
```

#### Parameters

| Parameter | Description                    | Type            | Default    |
| --------- | ------------------------------ | --------------- | ---------- |
| type      | The type of the item to verify | `InventoryItem` | `required` |
| number    | The number of items to verify  | `number`        | `required` |

---

## getStockInventoryItem()

Get the stock of an item from the inventory.

### Use in app module

```typescript
import { getStockInventoryItem } from '@bubble/store';

getStockInventoryItem({
  type: 'InventoryItem',
});
```

### Use in controller module

```typescript
import { socket } from '@bubble/common';

socket().emit('getStockInventoryItem', {
  type: 'InventoryItem',
});
```

#### Parameters

| Parameter | Description                    | Type            | Default    |
| --------- | ------------------------------ | --------------- | ---------- |
| type      | The type of the item to verify | `InventoryItem` | `required` |
