# Health Vital

## setHealth()

Set the health of the Bubble.

```typescript
import { setHealth } from "@bubble/store";

setHealth({
  value: 42,
});
```

#### Parameters

| Parameter | Description              | Type     | Default    |
| --------- | ------------------------ | -------- | ---------- |
| value     | The health of the Bubble | `number` | `required` |

---

## resetHealth()

Reset the health of the Bubble.

```typescript
import { resetHealth } from "@bubble/store";

resetHealth();
```

---

## addHealth()

Add to the health of the Bubble.

```typescript
import { addHealth } from "@bubble/store";

addHealth({
  value: 42,
});
```

#### Parameters

| Parameter | Description              | Type     | Default    |
| --------- | ------------------------ | -------- | ---------- |
| value     | The health of the Bubble | `number` | `required` |

---

## removeHealth()

Remove from the health of the Bubble.

```typescript
import { removeHealth } from "@bubble/store";

removeHealth({
  value: 42,
});
```

#### Parameters

| Parameter | Description              | Type     | Default    |
| --------- | ------------------------ | -------- | ---------- |
| value     | The health of the Bubble | `number` | `required` |
