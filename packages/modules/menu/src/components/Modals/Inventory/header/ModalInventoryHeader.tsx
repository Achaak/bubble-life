import { styled } from '@bubble/styles'
import React from 'react'

const HeaderContainer = styled('div', {})

export const ModalInventoryHeader = (): JSX.Element => {
  return <HeaderContainer>Inventory</HeaderContainer>
}
