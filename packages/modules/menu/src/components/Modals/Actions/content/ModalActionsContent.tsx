import { styled } from '@bubble/styles'
import React from 'react'

const ContentContainer = styled('div', {})

export const ModalActionsContent = (): JSX.Element => {
  return <ContentContainer>azdazdzd</ContentContainer>
}
