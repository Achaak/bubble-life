import { styled } from '@bubble/styles'
import React from 'react'

const HeaderContainer = styled('div', {})

export const ModalStatsHeader = (): JSX.Element => {
  return <HeaderContainer>Statistics</HeaderContainer>
}
